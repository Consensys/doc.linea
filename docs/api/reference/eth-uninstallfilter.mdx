---
title: eth_uninstallFilter
description: Removes a filter from the Linea RPC node.
image: /img/socialCards/ethuninstallfilter.jpg
---

import methods from '@site/static/data/rpc-methods.json';

export const m = methods.eth_uninstallFilter;

# `eth_uninstallFilter`

{m.description}

## Parameters

- `filterId`: _\[required]_ The filter ID to remove.

## Returns

{m.returns}

## Example

### Request

```bash
curl https://rpc.linea.build \
  -X POST \
  -H "Content-Type: application/json" \
  -d '{
    "jsonrpc": "2.0",
    "method": "eth_uninstallFilter",
    "params": ["0x10ff687202f305efab..."],
    "id": 1
  }'
```

### Response

```json
{
  "jsonrpc": "2.0",
  "id": 1,
  "result": true
}
```
