---
title: eth_getBlockTransactionCountByNumber
description: 'Returns the number of transactions in a Linea block, given the block number.'
image: /img/socialCards/ethgetblocktransactioncountbynumber.jpg
---

import RpcPlayground from '@site/src/components/RpcPlayground';

# `eth_getBlockTransactionCountByNumber`

Returns the number of transactions in a block, given the block number.

## Parameters

- `blockParameter`: _\[required]_ Hexadecimal block number, or `latest`, `earliest`, `pending`, `finalized`.

## Returns

Hexadecimal integer of the transaction count.

## Example

<RpcPlayground
  method="eth_getBlockTransactionCountByNumber"
  params={["0x1ce53dd"]}
  exampleResponse={{
    jsonrpc: "2.0",
    id: 1,
    result: "0x1"
  }}
/>
