---
title: eth_accounts
description: Returns a list of addresses owned by the connected node.
image: /img/socialCards/ethaccounts.jpg
---

import RpcPlayground from '@site/src/components/RpcPlayground';

# `eth_accounts`

Returns a list of addresses owned by the node. On the public Linea RPC endpoint the node does not
manage any accounts, so this method always returns an empty array.

## Parameters

None.

## Returns

Array of 20-byte addresses owned by the node. On `rpc.linea.build` this is always `[]`.

## Example

<RpcPlayground
  method="eth_accounts"
  params={[]}
  exampleResponse={{
    jsonrpc: "2.0",
    id: 1,
    result: []
  }}
/>
