---
title: eth_getBlockByNumber
description: Returns block information on Linea by block number.
image: /img/socialCards/ethgetblockbynumber.jpg
---

import RpcPlayground from '@site/src/components/RpcPlayground';
import methods from '@site/static/data/rpc-methods.json';

export const m = methods.eth_getBlockByNumber;

# `eth_getBlockByNumber`

{m.description}

## Parameters

- `blockParameter`: _\[required]_ Hexadecimal block number, or `latest`, `earliest`, `pending`, `finalized`.
- `fullTransactions`: _\[required]_ If `true`, returns full transaction objects. If `false`, returns only transaction hashes.

## Returns

{m.returns}

## Example

<RpcPlayground method={m.method} params={m.params} exampleResponse={m.exampleResponse} />
