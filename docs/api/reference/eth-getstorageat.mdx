---
title: eth_getStorageAt
description: Returns the value stored at a given storage slot of a contract on Linea.
image: /img/socialCards/ethgetstorageat.jpg
---

import RpcPlayground from '@site/src/components/RpcPlayground';
import methods from '@site/static/data/rpc-methods.json';

export const m = methods.eth_getStorageAt;

# `eth_getStorageAt`

{m.description}

## Parameters

- `address`: _\[required]_ 20-byte address of the contract.
- `position`: _\[required]_ Hexadecimal integer of the storage slot position.
- `blockParameter`: _\[required]_ Hexadecimal block number, or `latest`, `earliest`, `pending`, `finalized`.

## Returns

{m.returns}

## Example

This example reads the 32-byte value at storage position `0x0` of the USDC contract.

<RpcPlayground method={m.method} params={m.params} exampleResponse={m.exampleResponse} />
