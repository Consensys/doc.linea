---
title: Capabilities
description: Capabilities and evidence paths for evaluating a Lineth deployment
sidebar_position: 0
image: /img/socialCards/features.jpg
---

import DocCardList from "@theme/DocCardList";

export const relatedDocs = [
  {
    type: "link",
    href: "/stack/how-it-works/deployment-models",
    label: "Deployment models",
    description:
      "Compare public and private validium deployment models and their trade-offs.",
  },
  {
    type: "link",
    href: "/stack/how-it-works/data-availability-finalization",
    label: "Data availability and finalization",
    description:
      "How deployment choices affect data availability and finalization.",
  },
];

Use this page to scan Lineth capabilities that matter when evaluating a
deployment, and to find the pages that go deeper on each one. For design choices
and operator responsibilities, continue to
[deployment architecture](../how-it-works/index).

## What Lineth provides

Lineth is a ZK-rollup stack. A deployment combines a consensus layer, an
execution layer, a Prover, a Coordinator, and a set of onchain contracts on a
finalization layer. Operators choose the deployment model, data availability
arrangement, finalization layer, and access controls.

The capabilities below are scoped by the deployment model an operator chooses.
None of them is a product, SLA, or commercial packaging claim.

## Deployment model capabilities

Using Lineth, an operator can deploy:

- Public networks, such as Linea Mainnet, with onchain data availability
- Private validium networks, with offchain data availability and controlled
  access
- Custom L2 or L3 networks that finalize on Ethereum or on Linea Mainnet

For the trade-offs between these models, see
[Deployment models](../how-it-works/deployment-models).

## Protocol capabilities

### State transition correctness

Lineth uses zk-SNARK proofs to verify that state transitions are valid. A valid
proof means the operator cannot finalize an invalid state transition through the
normal proof path. Proofs do not by themselves make transaction contents
private; data visibility depends on the deployment's data availability and
access model. For the data-visibility distinction, see
[Privacy and data visibility](./validium).

### EVM-compatible execution

Lineth runs EVM-compatible contract execution, so existing Solidity contracts
and Ethereum tooling generally work. Compatibility is constrained by the
deployment's configuration and by the differences between any chain and
Ethereum. For the differences between Linea and Ethereum, see
[Linea and Ethereum](../../network/overview/ethereum-differences).

### Quorum-Based Byzantine Fault Tolerance consensus

For deployments that use multi-validator consensus, Lineth supports Quorum-Based
Byzantine Fault Tolerance (QBFT), which provides:

- Fault tolerance with `3f+1` validators, tolerating `f` faulty validators
- Single-slot finality under the deployment's QBFT safety assumptions
- Distributed block production when enough validators participate

Single-validator deployments do not use QBFT and rely on the operator trust
model instead. For the trust assumptions per model, see
[Trust and responsibilities](../how-it-works/trust-model).

### Dual-layer architecture

Lineth separates consensus and execution. This lets operators choose different
client configurations and scale consensus and execution independently. It does
not by itself provide client diversity or network resilience; those depend on
the operator's deployment.

## Where to go next

| Question                                                        | Page                                                                                     |
| --------------------------------------------------------------- | ---------------------------------------------------------------------------------------- |
| Who operates what, and what can each component do?              | [Trust and responsibilities](../how-it-works/trust-model)                            |
| Who can see what data?                                          | [Privacy and data visibility](./validium)                                            |
| What is protocol versus operator, and what evidence exists?     | [Security and assurance](./security)                                                 |
| What protocol evidence can operators use for compliance design? | [Compliance](./compliance)                                                           |
| How do data availability and finalization choices interact?     | [Data availability and finalization](../how-it-works/data-availability-finalization) |
| How is access to RPC and APIs controlled?                       | [Access control](./rbac)                                                             |

## Related documentation

<DocCardList items={relatedDocs} />
