---
title: How do I bridge ERC20 tokens?
sidebar_position: 2
---


:::note

We recommend that only tech operators that are providing liquidity use our bridge for ERC20 tokens. For everyone else, please take a look at our [ecosystem portal](https://linea.build/apps) and filter for bridges.
**ERC20 tokens can only be bridged via manual claiming, which requires you to cover the ETH fee on the destination layer. Make sure your wallet is properly funded before beginning this procees! Checkout the [tutorial](/use-mainnet/bridges-of-linea/how-to-bridge-Eth) on how to bridge ETH to learn how to use our bridge and appropriately fund your wallet.**

:::



1. On the [token-bridge](https://bridge.linea.build/) go to the token selection menu by clicking on the current token symbol (ETH in this case).

<img
  src={
    require("@site/static/img/docs/use-mainnet/bridges-of-linea/token_selection_menu.png")
      .default
  }></img>


2. Select one of the available tokens in the list. If the token is not in the list, paste the address of the token you want to add into the search bar.

<img
  src={
    require("@site/static/img/docs/use-mainnet/bridges-of-linea/token_list.png")
      .default    
  }></img>


4. Click on the token to select it.

<img
  src={
    require("@site/static/img/docs/use-mainnet/bridges-of-linea/dogecoin.png")
      .default
  }></img>


5. You now can bridge this token and it’s saved in your token list for future bridging.

<img
  src={
    require("@site/static/img/docs/use-mainnet/bridges-of-linea/doge_bridging.png")
      .default
  }></img>
