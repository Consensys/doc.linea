---
title: Prover
description: How the Lineth prover turns execution traces into zero-knowledge proofs.
sidebar_position: 14
image: /img/socialCards/prover.jpg
---

import GlossaryTerm from '@theme/GlossaryTerm';

The prover is a <GlossaryTerm term="Lineth"/> module that turns execution traces into
<GlossaryTerm term="zk-SNARK">zk-SNARKs</GlossaryTerm>.
These zk-SNARKs enable the <GlossaryTerm term="Finalization layer">finalization layer</GlossaryTerm> to
accept a Lineth state transition without re-executing any transactions.

Proof generation is computationally intensive, so deployments run the prover as one or more dedicated
instances rather than inside the execution client.

## How it works

The [coordinator](../coordinator/index.mdx) drives the prover.
It writes each proof request, along with the conflated traces produced by the
[tracer](../sequencer/traces-generator.mdx), to the directories the prover watches, and collects the
finished proof from the same place.

The prover produces three kinds of proofs:

- **Execution proofs** show that the transactions in a batch executed correctly.
- **Compression proofs** show that a blob's compressed data expands to the batches it stands for.
- **Aggregation proofs** combine the proofs for several blobs into the single proof submitted at finalization.

Each proof is built in two stages:

1. [Corset](trace-expansion.mdx) expands the trace data into the full constraint system, the
    arithmetization the prover proves against.
2. The proving system reduces that expanded data to an inner proof, then wraps it with the
    [gnark](proving.mdx) library into an outer proof: the zk-SNARK that a verifier contract on the
    finalization layer can check cheaply.

Because the proof accompanies the data it proves, anyone can confirm that a state transition is correct
by checking the proof onchain, instead of trusting the operator or re-executing the transactions themselves.

## See also

- See [Circuit building](./trace-expansion.mdx) and
  [Circuit execution and runtime](./proving.mdx) for how Corset and gnark each work in detail.
- See [Module limits](./prover-limits.mdx) for the trace line limits that keep batches provable, and
  [Trusted setup](./trusted-setup.mdx) for the reference strings the proving system relies on.
- See [RISC-V proving architecture](./risc-v-overview.mdx) for the planned evolution toward
  guest-program-based proof generation.
- Configure prover behavior using the
  [Linea prover options](../../../stack/reference/linea-prover-options.mdx).
- See the [prover source code](https://github.com/LFDT-Lineth/lineth-monorepo/tree/main/prover) in
  the `lineth-monorepo`.
