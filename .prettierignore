# Automation-owned generated content. Seeded from the lineth-monorepo artifact and refreshed by
# the coordinator-config-docs workflow; prettier would reformat tables/prose and create churn
# against the generator output, so the namespace is exempt from formatting checks.
docs/stack/reference/_generated/coordinator/
